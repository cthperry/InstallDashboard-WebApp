# F53 修正摘要

版號：20260403-F53

## 本次修正

1. 機台帳稼動率改為依 `UPH / Target UPH * 100` 自動換算。
2. 開啟設備編輯表單時，會即時重算舊資料的稼動率，不再直接信任資料庫舊值。
3. 調整 UPH / Target UPH 時，表單中的稼動率會即時同步更新。
4. 存檔時稼動率會再次重算後寫回，避免舊錯值殘留。
5. 戰情室「地區產品產能」移除 `K` 縮寫，改為顯示原始數值與 `UPH` 單位。
6. 戰情室設備平均稼動率改為使用即時計算值。
7. 版號同步更新為 `20260403-F53`。
