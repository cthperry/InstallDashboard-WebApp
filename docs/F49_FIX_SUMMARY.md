# F49 修正摘要

- 版號顯示來源收斂為單一來源：`package.json.version`，並由 `src/config/appVersion.ts` 統一輸出。
- 同步更新 `package-lock.json` 版本，避免檔案內外版本不同步。
- Excel 智慧匯入預覽視窗改為更寬、更緊湊的表格配置。
- 下修表格最小寬度與各欄固定寬度，讓最後日期欄位在常見桌面寬度下可完整顯示。
- 預覽 modal 內容 padding 收斂，增加可視空間。
